package selenium;

public interface InterfaceNew {
	int a = 10; //interface variables are final and static by default (vFS)
	void display();
	void show(); //interface methods are abstract and public by default (mAP)
}
